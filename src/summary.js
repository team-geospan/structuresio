/**
 * @module/structuresio/summary
 * Summarize StructuresJSON into table friendly data structures.
 */

export const summarizeByMaterialAndPitch = collection => {
  const summary = {};

  return summary;
}
